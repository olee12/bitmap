# Bitmap Benchmark

```text
1. Roaring Bitmap (roaring)
2. Sparse Bitmap (golang map[uint64]uint64)
3. Bitmap using golang map with roaring-bitmap idea to 
    improve the access time compared to original Roaring Bitmap and,
    improve memory compared to Sparse Bitmap
```


